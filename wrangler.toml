name = "housekeep-os"
pages_build_output_dir = "dist"

[vars]
# These can be overridden in the Cloudflare Dashboard
DEBUG = "false"
# GOOGLE_SHEET_ID and GOOGLE_SERVICE_ACCOUNT_JSON should be set via Cloudflare Secrets

[node_compat]
# Required for libraries like googleapis that use Node.js built-ins
main = true
